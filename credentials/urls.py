"""
URL configuration for credentials app.
"""

from django.urls import path

app_name = 'credentials'

urlpatterns = [
    # Placeholder for credentials URLs
    # TODO: Add credential management URLs when views are implemented
]
