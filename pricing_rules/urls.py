"""
URL configuration for pricing_rules app.
"""

from django.urls import path

app_name = 'pricing_rules'

urlpatterns = [
    # Placeholder for pricing rules URLs
    # TODO: Add pricing rules management URLs when views are implemented
]
